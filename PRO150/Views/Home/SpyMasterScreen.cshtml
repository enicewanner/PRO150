@model Card

<div class="text-center">
    <h2>You Are A Spy Master</h2>
    </br>

    <a href="GuesserTran"><button class="btn btn-primary">I've Given My Hint</button></a>
</div>


<div class="container text-center">
    <div class="row p-2">
        <div class="col border-2 border-primary p-5 m-3">
          
            @if(!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete )
            {
                 Card.color1 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color1">
                    Column
                </div>

                switch (Card.color1)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color1">
                    Column
                </div>
            }

              
            
        </div>
        <div class="col border-2 border-primary p-5 m-3">

            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color2 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color2">
                    Column
                </div>

                switch (Card.color2)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color2 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color2">
                    Column
                </div>
                 switch (Card.color2)
                {
                    
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color2 = @Card.colors[Card.rand.Next(4)];
                while (Card.color2.Equals("Red"))
                {
                    Card.color2 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color2">
                    Column
                </div>

                  switch (Card.color2)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                   
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color2 = @Card.colors[Card.rand.Next(4)];
                while (Card.color2.Equals("Blue"))
                {
                    Card.color2 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color2">
                    Column
                </div>

                 switch (Card.color2)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color2 = @Card.colors[Card.rand.Next(4)];
                while (Card.color2.Equals("White"))
                {
                    Card.color2 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color2">
                    Column
                </div>

                 switch (Card.color2)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color2 = @Card.colors[Card.rand.Next(3)];
                while (Card.color2.Equals("Red"))
                {
                    Card.color2 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color2">
                    Column
                </div>

                

                 switch (Card.color2)
                {
                   
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color2 = @Card.colors[Card.rand.Next(3)];
                while (Card.color2.Equals("Blue"))
                {
                    Card.color2 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color2">
                    Column
                </div>

                 switch (Card.color2)
                {
                   
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                   
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color2 = @Card.colors[Card.rand.Next(3)];
                while (Card.color2.Equals("White"))
                {
                    Card.color2 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color2">
                    Column
                </div>

                 switch (Card.color2)
                {
                  
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                   


                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color2 = @Card.colors[Card.rand.Next(4)];
                while (Card.color2.Equals("Red") || Card.color2.Equals("Blue"))
                {
                    Card.color2 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color2">
                    Column
                </div>

                switch (Card.color2)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                  
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color2 = @Card.colors[Card.rand.Next(4)];
                while (Card.color2.Equals("Red") || Card.color2.Equals("White"))
                {
                    Card.color2 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color2">
                    Column
                </div>

                 switch (Card.color2)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                  
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color2 = @Card.colors[Card.rand.Next(4)];
                while (Card.color2.Equals("White") || Card.color2.Equals("Blue"))
                {
                    Card.color2 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color2">
                    Column
                </div>

                switch (Card.color2)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color2 = "Red";


                <div class="@Card.color2">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color2 = "Blue";


                <div class="@Card.color2">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color2 = "White";


                <div class="@Card.color2">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color2 = "Black";


                <div class="@Card.color2">
                    Column
                </div>


                if (Card.color2.Equals("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color2">
                    Column
                </div>
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color3 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color3">
                    Column
                </div>

                switch (Card.color3)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color3 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color3">
                    Column
                </div>
                switch (Card.color3)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color3 = @Card.colors[Card.rand.Next(4)];
                while (Card.color3.Equals("Red"))
                {
                    Card.color3 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color3">
                    Column
                </div>

                switch (Card.color3)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color3 = @Card.colors[Card.rand.Next(4)];
                while (Card.color3.Equals("Blue"))
                {
                    Card.color3 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color3">
                    Column
                </div>

                switch (Card.color3)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color3 = @Card.colors[Card.rand.Next(4)];
                while (Card.color3.Contains("White"))
                {
                    Card.color3 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color3">
                    Column
                </div>

                switch (Card.color3)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color3 = @Card.colors[Card.rand.Next(3)];
                while (Card.color3.Equals("Red"))
                {
                    Card.color3 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color3">
                    Column
                </div>



                switch (Card.color3)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color3 = @Card.colors[Card.rand.Next(3)];
                while (Card.color3.Equals("Blue"))
                {
                    Card.color3 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color3">
                    Column
                </div>

                switch (Card.color3)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color3 = @Card.colors[Card.rand.Next(3)];
                while (Card.color3.Equals("White"))
                {
                    Card.color3 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color3">
                    Column
                </div>

                switch (Card.color3)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color3 = @Card.colors[Card.rand.Next(4)];
                while (Card.color3.Equals("Red") || Card.color3.Equals("Blue"))
                {
                    Card.color3 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color3">
                    Column
                </div>

                switch (Card.color3)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color3 = @Card.colors[Card.rand.Next(4)];
                while (Card.color3.Equals("Red") || Card.color3.Equals("White"))
                {
                    Card.color3 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color3">
                    Column
                </div>

                switch (Card.color3)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color3 = @Card.colors[Card.rand.Next(4)];
                while (Card.color3.Equals("White") || Card.color3.Equals("Blue"))
                {
                    Card.color3 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color3">
                    Column
                </div>

                switch (Card.color3)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color3 = "Red";


                <div class="@Card.color3">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color3 = "Blue";


                <div class="@Card.color3">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color3 = "White";


                <div class="@Card.color3">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color3 = "Black";


                <div class="@Card.color3">
                    Column
                </div>


                if (Card.color3.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color3">
                    Column
                </div>
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color4 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color4">
                    Column
                </div>

                switch (Card.color4)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color4 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color4">
                    Column
                </div>
                switch (Card.color4)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color4 = @Card.colors[Card.rand.Next(4)];
                while (Card.color4.Contains("Red"))
                {
                    Card.color4 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color4">
                    Column
                </div>

                switch (Card.color4)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color4 = @Card.colors[Card.rand.Next(4)];
                while (Card.color4.Contains("Blue"))
                {
                    Card.color4 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color4">
                    Column
                </div>

                switch (Card.color4)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color4 = @Card.colors[Card.rand.Next(4)];
                while (Card.color4.Contains("White"))
                {
                    Card.color4 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color4">
                    Column
                </div>

                switch (Card.color4)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color4 = @Card.colors[Card.rand.Next(3)];
                while (Card.color4.Equals("Red"))
                {
                    Card.color4 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color4">
                    Column
                </div>



                switch (Card.color4)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color4 = @Card.colors[Card.rand.Next(3)];
                while (Card.color4.Equals("Blue"))
                {
                    Card.color4 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color4">
                    Column
                </div>

                switch (Card.color4)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color4 = @Card.colors[Card.rand.Next(3)];
                while (Card.color4.Equals("White"))
                {
                    Card.color4 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color4">
                    Column
                </div>

                switch (Card.color4)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color4 = @Card.colors[Card.rand.Next(4)];
                while (Card.color4.Equals("Red") || Card.color4.Equals("Blue"))
                {
                    Card.color4 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color4">
                    Column
                </div>

                switch (Card.color4)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color4 = @Card.colors[Card.rand.Next(4)];
                while (Card.color4.Equals("Red") || Card.color4.Equals("White"))
                {
                    Card.color4 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color4">
                    Column
                </div>

                switch (Card.color4)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color4 = @Card.colors[Card.rand.Next(4)];
                while (Card.color4.Equals("White") || Card.color4.Equals("Blue"))
                {
                    Card.color4 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color4">
                    Column
                </div>

                switch (Card.color4)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color4 = "Red";


                <div class="@Card.color4">
                    Column
                </div>


                if (Card.color4.Contains("Red"))
                {
                    Card.redColorCount++;
                    if (Card.redColorCount == 9)
                    {
                        Card.redComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color4 = "Blue";


                <div class="@Card.color4">
                    Column
                </div>


                if (Card.color4.Contains("Blue"))
                {
                    Card.blueColorCount++;
                    if (Card.blueColorCount == 8)
                    {
                        Card.blueComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color4 = "White";


                <div class="@Card.color4">
                    Column
                </div>


                if (Card.color4.Contains("White"))
                {
                    Card.whiteColorCount++;
                    if (Card.whiteColorCount == 7)
                    {
                        Card.whiteComplete = true;
                    }
                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color4 = "Black";


                <div class="@Card.color4">
                    Column
                </div>


                if (Card.color4.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color4">
                    Column
                </div>
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color5 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color5">
                    Column
                </div>

                switch (Card.color5)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color5 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color5">
                    Column
                </div>
                switch (Card.color5)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color5 = @Card.colors[Card.rand.Next(4)];
                while (Card.color5.Contains("Red"))
                {
                    Card.color5 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color5">
                    Column
                </div>

                switch (Card.color5)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color5 = @Card.colors[Card.rand.Next(4)];
                while (Card.color5.Contains("Blue"))
                {
                    Card.color5 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color5">
                    Column
                </div>

                switch (Card.color5)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color5 = @Card.colors[Card.rand.Next(4)];
                while (Card.color5.Contains("White"))
                {
                    Card.color5 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color5">
                    Column
                </div>

                switch (Card.color5)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color5 = @Card.colors[Card.rand.Next(3)];
                while (Card.color5.Equals("Red"))
                {
                    Card.color5 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color5">
                    Column
                </div>



                switch (Card.color5)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color5 = @Card.colors[Card.rand.Next(3)];
                while (Card.color5.Equals("Blue"))
                {
                    Card.color5 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color5">
                    Column
                </div>

                switch (Card.color5)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color5 = @Card.colors[Card.rand.Next(3)];
                while (Card.color5.Equals("White"))
                {
                    Card.color5 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color5">
                    Column
                </div>

                switch (Card.color5)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color5 = @Card.colors[Card.rand.Next(4)];
                while (Card.color5.Equals("Red") || Card.color5.Equals("Blue"))
                {
                    Card.color5 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color5">
                    Column
                </div>

                switch (Card.color5)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color5 = @Card.colors[Card.rand.Next(4)];
                while (Card.color5.Equals("Red") || Card.color5.Equals("White"))
                {
                    Card.color5 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color5">
                    Column
                </div>

                switch (Card.color5)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color5 = @Card.colors[Card.rand.Next(4)];
                while (Card.color5.Equals("White") || Card.color5.Equals("Blue"))
                {
                    Card.color5 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color5">
                    Column
                </div>

                switch (Card.color5)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color5 = "Red";


                <div class="@Card.color5">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color5 = "Blue";


                <div class="@Card.color5">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color5 = "White";


                <div class="@Card.color5">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color5 = "Black";


                <div class="@Card.color5">
                    Column
                </div>


                if (Card.color5.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color5">
                    Column
                </div>
            }

        </div>
    </div>
    <div class="row p-2">
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color6 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color6">
                    Column
                </div>

                switch (Card.color6)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color6 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color6">
                    Column
                </div>
                switch (Card.color6)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color6 = @Card.colors[Card.rand.Next(4)];
                while (Card.color6.Contains("Red"))
                {
                    Card.color6 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color6">
                    Column
                </div>

                switch (Card.color6)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color6 = @Card.colors[Card.rand.Next(4)];
                while (Card.color6.Contains("Blue"))
                {
                    Card.color6 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color6">
                    Column
                </div>

                switch (Card.color6)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color6 = @Card.colors[Card.rand.Next(4)];
                while (Card.color6.Contains("White"))
                {
                    Card.color6 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color6">
                    Column
                </div>

                switch (Card.color6)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color6 = @Card.colors[Card.rand.Next(3)];
                while (Card.color6.Equals("Red"))
                {
                    Card.color6 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color6">
                    Column
                </div>



                switch (Card.color6)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color6 = @Card.colors[Card.rand.Next(3)];
                while (Card.color6.Equals("Blue"))
                {
                    Card.color6 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color6">
                    Column
                </div>

                switch (Card.color6)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color6 = @Card.colors[Card.rand.Next(3)];
                while (Card.color6.Equals("White"))
                {
                    Card.color6 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color6">
                    Column
                </div>

                switch (Card.color6)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color6 = @Card.colors[Card.rand.Next(4)];
                while (Card.color6.Equals("Red") || Card.color6.Equals("Blue"))
                {
                    Card.color6 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color6">
                    Column
                </div>

                switch (Card.color6)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color6 = @Card.colors[Card.rand.Next(4)];
                while (Card.color6.Equals("Red") || Card.color6.Equals("White"))
                {
                    Card.color6 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color6">
                    Column
                </div>

                switch (Card.color6)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color6 = @Card.colors[Card.rand.Next(4)];
                while (Card.color6.Equals("White") || Card.color6.Equals("Blue"))
                {
                    Card.color6 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color6">
                    Column
                </div>

                switch (Card.color6)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color6 = "Red";


                <div class="@Card.color6">
                    Column
                </div>


                if (Card.color6.Contains("Red"))
                {
                    Card.redColorCount++;
                    if (Card.redColorCount == 9)
                    {
                        Card.redComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color6 = "Blue";


                <div class="@Card.color6">
                    Column
                </div>


                if (Card.color6.Contains("Blue"))
                {
                    Card.blueColorCount++;
                    if (Card.blueColorCount == 8)
                    {
                        Card.blueComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
               Card.color6 = "White";


                <div class="@Card.color6">
                    Column
                </div>


                if (Card.color6.Contains("White"))
                {
                    Card.whiteColorCount++;
                    if (Card.whiteColorCount == 7)
                    {
                        Card.whiteComplete = true;
                    }
                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color6 = "Black";


                <div class="@Card.color6">
                    Column
                </div>


                if (Card.color6.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color6">
                    Column
                </div>
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color7 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color7">
                    Column
                </div>

                switch (Card.color7)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color7 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color7">
                    Column
                </div>
                switch (Card.color7)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color7 = @Card.colors[Card.rand.Next(4)];
                while (Card.color7.Contains("Red"))
                {
                    Card.color7 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color7">
                    Column
                </div>

                switch (Card.color7)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color7 = @Card.colors[Card.rand.Next(4)];
                while (Card.color7.Contains("Blue"))
                {
                    Card.color7 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color7">
                    Column
                </div>

                switch (Card.color7)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color7 = @Card.colors[Card.rand.Next(4)];
                while (Card.color7.Contains("White"))
                {
                    Card.color7 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color7">
                    Column
                </div>

                switch (Card.color7)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color7 = @Card.colors[Card.rand.Next(3)];
                while (Card.color7.Equals("Red"))
                {
                    Card.color7 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color7">
                    Column
                </div>



                switch (Card.color7)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color7 = @Card.colors[Card.rand.Next(3)];
                while (Card.color7.Equals("Blue"))
                {
                    Card.color7 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color7">
                    Column
                </div>

                switch (Card.color7)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color7 = @Card.colors[Card.rand.Next(3)];
                while (Card.color7.Equals("White"))
                {
                    Card.color7 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color7">
                    Column
                </div>

                switch (Card.color7)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color7 = @Card.colors[Card.rand.Next(4)];
                while (Card.color7.Equals("Red") || Card.color7.Equals("Blue"))
                {
                    Card.color7 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color7">
                    Column
                </div>

                switch (Card.color7)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color7 = @Card.colors[Card.rand.Next(4)];
                while (Card.color7.Equals("Red") || Card.color7.Equals("White"))
                {
                    Card.color7 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color7">
                    Column
                </div>

                switch (Card.color7)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color7 = @Card.colors[Card.rand.Next(4)];
                while (Card.color7.Equals("White") || Card.color7.Equals("Blue"))
                {
                    Card.color7 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color7">
                    Column
                </div>

                switch (Card.color7)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color7 = "Red";


                <div class="@Card.color7">
                    Column
                </div>


                if (Card.color7.Contains("Red"))
                {
                    Card.redColorCount++;
                    if (Card.redColorCount == 9)
                    {
                        Card.redComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color7 = "Red";


                <div class="@Card.color7">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color7 = "Blue";


                <div class="@Card.color7">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color7 = "White";


                <div class="@Card.color7">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color7 = "Black";


                <div class="@Card.color7">
                    Column
                </div>


                if (Card.color7.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color7">
                    Column
                </div>
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color8 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color8">
                    Column
                </div>

                switch (Card.color8)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color8 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color8">
                    Column
                </div>
                switch (Card.color8)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color8 = @Card.colors[Card.rand.Next(4)];
                while (Card.color8.Contains("Red"))
                {
                    Card.color8 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color8">
                    Column
                </div>

                switch (Card.color8)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color8 = @Card.colors[Card.rand.Next(4)];
                while (Card.color8.Contains("Blue"))
                {
                    Card.color8 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color8">
                    Column
                </div>

                switch (Card.color8)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color8 = @Card.colors[Card.rand.Next(4)];
                while (Card.color8.Contains("White"))
                {
                    Card.color8 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color8">
                    Column
                </div>

                switch (Card.color8)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color8 = @Card.colors[Card.rand.Next(3)];
                while (Card.color8.Equals("Red"))
                {
                    Card.color8 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color8">
                    Column
                </div>



                switch (Card.color8)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color8 = @Card.colors[Card.rand.Next(3)];
                while (Card.color8.Equals("Blue"))
                {
                    Card.color8 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color8">
                    Column
                </div>

                switch (Card.color8)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color8 = @Card.colors[Card.rand.Next(3)];
                while (Card.color8.Equals("White"))
                {
                    Card.color8 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color8">
                    Column
                </div>

                switch (Card.color8)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color8 = @Card.colors[Card.rand.Next(4)];
                while (Card.color8.Equals("Red") || Card.color8.Equals("Blue"))
                {
                    Card.color8 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color8">
                    Column
                </div>

                switch (Card.color8)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color8 = @Card.colors[Card.rand.Next(4)];
                while (Card.color8.Equals("Red") || Card.color8.Equals("White"))
                {
                    Card.color8 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color8">
                    Column
                </div>

                switch (Card.color8)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color8 = @Card.colors[Card.rand.Next(4)];
                while (Card.color8.Equals("White") || Card.color8.Equals("Blue"))
                {
                    Card.color8 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color8">
                    Column
                </div>

                switch (Card.color8)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color8 = "Red";


                <div class="@Card.color8">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color8 = "Blue";


                <div class="@Card.color8">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color8 = "White";


                <div class="@Card.color8">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color8 = "Black";


                <div class="@Card.color8">
                    Column
                </div>


                if (Card.color8.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color8">
                    Column
                </div>
            }
        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color9 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color9">
                    Column
                </div>

                switch (Card.color9)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color9 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color9">
                    Column
                </div>
                switch (Card.color9)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color9 = @Card.colors[Card.rand.Next(4)];
                while (Card.color9.Contains("Red"))
                {
                    Card.color9 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color9">
                    Column
                </div>

                switch (Card.color9)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color9 = @Card.colors[Card.rand.Next(4)];
                while (Card.color9.Contains("Blue"))
                {
                    Card.color9 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color9">
                    Column
                </div>

                switch (Card.color9)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color9 = @Card.colors[Card.rand.Next(4)];
                while (Card.color9.Contains("White"))
                {
                    Card.color9 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color9">
                    Column
                </div>

                switch (Card.color9)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color9 = @Card.colors[Card.rand.Next(3)];
                while (Card.color9.Equals("Red"))
                {
                    Card.color9 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color9">
                    Column
                </div>



                switch (Card.color9)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color9 = @Card.colors[Card.rand.Next(3)];
                while (Card.color9.Equals("Blue"))
                {
                    Card.color9 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color9">
                    Column
                </div>

                switch (Card.color9)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color9 = @Card.colors[Card.rand.Next(3)];
                while (Card.color9.Equals("White"))
                {
                    Card.color9 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color9">
                    Column
                </div>

                switch (Card.color9)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color9 = @Card.colors[Card.rand.Next(4)];
                while (Card.color9.Equals("Red") || Card.color9.Equals("Blue"))
                {
                    Card.color9 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color9">
                    Column
                </div>

                switch (Card.color9)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color9 = @Card.colors[Card.rand.Next(4)];
                while (Card.color9.Equals("Red") || Card.color9.Equals("White"))
                {
                    Card.color9 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color9">
                    Column
                </div>

                switch (Card.color9)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color9 = @Card.colors[Card.rand.Next(4)];
                while (Card.color9.Equals("White") || Card.color9.Equals("Blue"))
                {
                    Card.color9 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color9">
                    Column
                </div>

                switch (Card.color9)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color9 = "Red";


                <div class="@Card.color9">
                    Column
                </div>


                if (Card.color9.Contains("Red"))
                {
                    Card.redColorCount++;
                    if (Card.redColorCount == 9)
                    {
                        Card.redComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color9 = "Blue";


                <div class="@Card.color9">
                    Column
                </div>


                if (Card.color9.Contains("Blue"))
                {
                    Card.blueColorCount++;
                    if (Card.blueColorCount == 8)
                    {
                        Card.blueComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color9 = "White";


                <div class="@Card.color9">
                    Column
                </div>


                if (Card.color9.Contains("White"))
                {
                    Card.whiteColorCount++;
                    if (Card.whiteColorCount == 7)
                    {
                        Card.whiteComplete = true;
                    }
                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
               Card.color9 = "Black";


                <div class="@Card.color9">
                    Column
                </div>


                if (Card.color9.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color9">
                    Column
                </div>
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color10 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color10">
                    Column
                </div>

                switch (Card.color10)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color10 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color10">
                    Column
                </div>
                switch (Card.color10)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color10 = @Card.colors[Card.rand.Next(4)];
                while (Card.color10.Contains("Red"))
                {
                    Card.color10 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color10">
                    Column
                </div>

                switch (Card.color10)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color10 = @Card.colors[Card.rand.Next(4)];
                while (Card.color10.Contains("Blue"))
                {
                    Card.color10 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color10">
                    Column
                </div>

                switch (Card.color10)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color10 = @Card.colors[Card.rand.Next(4)];
                while (Card.color10.Contains("White"))
                {
                    Card.color10 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color10">
                    Column
                </div>

                switch (Card.color10)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color10 = @Card.colors[Card.rand.Next(3)];
                while (Card.color10.Equals("Red"))
                {
                    Card.color10 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color10">
                    Column
                </div>



                switch (Card.color10)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color10 = @Card.colors[Card.rand.Next(3)];
                while (Card.color10.Equals("Blue"))
                {
                    Card.color10 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color10">
                    Column
                </div>

                switch (Card.color10)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color10 = @Card.colors[Card.rand.Next(3)];
                while (Card.color10.Equals("White"))
                {
                    Card.color10 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color10">
                    Column
                </div>

                switch (Card.color10)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color10 = @Card.colors[Card.rand.Next(4)];
                while (Card.color10.Equals("Red") || Card.color10.Equals("Blue"))
                {
                    Card.color10 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color10">
                    Column
                </div>

                switch (Card.color10)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color10 = @Card.colors[Card.rand.Next(4)];
                while (Card.color10.Equals("Red") || Card.color10.Equals("White"))
                {
                    Card.color10 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color10">
                    Column
                </div>

                switch (Card.color10)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color10 = @Card.colors[Card.rand.Next(4)];
                while (Card.color10.Equals("White") || Card.color10.Equals("Blue"))
                {
                    Card.color10 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color10">
                    Column
                </div>

                switch (Card.color10)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color10 = "Red";


                <div class="@Card.color10">
                    Column
                </div>


                if (Card.color10.Contains("Red"))
                {
                    Card.redColorCount++;
                    if (Card.redColorCount == 9)
                    {
                        Card.redComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color10 = "Blue";


                <div class="@Card.color10">
                    Column
                </div>


                if (Card.color10.Contains("Blue"))
                {
                    Card.blueColorCount++;
                    if (Card.blueColorCount == 8)
                    {
                        Card.blueComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color10 = "White";


                <div class="@Card.color10">
                    Column
                </div>


                if (Card.color10.Contains("White"))
                {
                    Card.whiteColorCount++;
                    if (Card.whiteColorCount == 7)
                    {
                        Card.whiteComplete = true;
                    }
                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color10 = "Black";


                <div class="@Card.color10">
                    Column
                </div>


                if (Card.color10.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color10">
                    Column
                </div>
            }

        </div>
    </div>
    <div class="row p-2">
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color11 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color11">
                    Column
                </div>

                switch (Card.color11)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color11 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color11">
                    Column
                </div>
                switch (Card.color11)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color11 = @Card.colors[Card.rand.Next(4)];
                while (Card.color11.Contains("Red"))
                {
                    Card.color11 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color11">
                    Column
                </div>

                switch (Card.color11)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color11 = @Card.colors[Card.rand.Next(4)];
                while (Card.color11.Contains("Blue"))
                {
                    Card.color11 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color11">
                    Column
                </div>

                switch (Card.color11)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color11 = @Card.colors[Card.rand.Next(4)];
                while (Card.color11.Contains("White"))
                {
                    Card.color11 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color11">
                    Column
                </div>

                switch (Card.color11)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color11 = @Card.colors[Card.rand.Next(3)];
                while (Card.color11.Equals("Red"))
                {
                    Card.color11 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color11">
                    Column
                </div>



                switch (Card.color11)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color11 = @Card.colors[Card.rand.Next(3)];
                while (Card.color11.Equals("Blue"))
                {
                    Card.color11 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color11">
                    Column
                </div>

                switch (Card.color11)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color11 = @Card.colors[Card.rand.Next(3)];
                while (Card.color11.Equals("White"))
                {
                    Card.color11 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color11">
                    Column
                </div>

                switch (Card.color11)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color11 = @Card.colors[Card.rand.Next(4)];
                while (Card.color11.Equals("Red") || Card.color11.Equals("Blue"))
                {
                    Card.color11 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color11">
                    Column
                </div>

                switch (Card.color11)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color11 = @Card.colors[Card.rand.Next(4)];
                while (Card.color11.Equals("Red") || Card.color11.Equals("White"))
                {
                    Card.color11 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color11">
                    Column
                </div>

                switch (Card.color11)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color11 = @Card.colors[Card.rand.Next(4)];
                while (Card.color11.Equals("White") || Card.color11.Equals("Blue"))
                {
                    Card.color11 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color11">
                    Column
                </div>

                switch (Card.color11)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
          else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color11 = "Red";
               

                <div class="@Card.color11">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }
                
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color11 = "Blue";


                <div class="@Card.color11">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }
                
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color11 = "White";


                <div class="@Card.color11">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }
                
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color11 = "Black";


                <div class="@Card.color11">
                    Column
                </div>


                if (Card.color11.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color11">
                    Column
                </div>
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color12 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color12">
                    Column
                </div>

                switch (Card.color12)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color12 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color12">
                    Column
                </div>
                switch (Card.color12)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color12 = @Card.colors[Card.rand.Next(4)];
                while (Card.color12.Contains("Red"))
                {
                    Card.color12 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color12">
                    Column
                </div>

                switch (Card.color12)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color12 = @Card.colors[Card.rand.Next(4)];
                while (Card.color12.Contains("Blue"))
                {
                    Card.color12 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color12">
                    Column
                </div>

                switch (Card.color12)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color12 = @Card.colors[Card.rand.Next(4)];
                while (Card.color12.Contains("White"))
                {
                    Card.color12 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color12">
                    Column
                </div>

                switch (Card.color12)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color12 = @Card.colors[Card.rand.Next(3)];
                while (Card.color12.Equals("Red"))
                {
                    Card.color12 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color12">
                    Column
                </div>



                switch (Card.color12)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color12 = @Card.colors[Card.rand.Next(3)];
                while (Card.color12.Equals("Blue"))
                {
                    Card.color12 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color12">
                    Column
                </div>

                switch (Card.color12)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color12 = @Card.colors[Card.rand.Next(3)];
                while (Card.color12.Equals("White"))
                {
                    Card.color12 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color12">
                    Column
                </div>

                switch (Card.color12)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color12 = @Card.colors[Card.rand.Next(4)];
                while (Card.color12.Equals("Red") || Card.color12.Equals("Blue"))
                {
                    Card.color12 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color12">
                    Column
                </div>

                switch (Card.color12)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color12 = @Card.colors[Card.rand.Next(4)];
                while (Card.color12.Equals("Red") || Card.color12.Equals("White"))
                {
                    Card.color12 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color12">
                    Column
                </div>

                switch (Card.color12)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color12 = @Card.colors[Card.rand.Next(4)];
                while (Card.color12.Equals("White") || Card.color12.Equals("Blue"))
                {
                    Card.color12 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color12">
                    Column
                </div>

                switch (Card.color12)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color12 = "Red";


                <div class="@Card.color12">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color12 = "Blue";


                <div class="@Card.color12">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color12 = "White";


                <div class="@Card.color12">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color12 = "Black";


                <div class="@Card.color12">
                    Column
                </div>


                if (Card.color12.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color12">
                    Column
                </div>
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">

            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color13 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color13">
                    Column
                </div>

                switch (Card.color13)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color13 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color13">
                    Column
                </div>
                switch (Card.color13)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color13 = @Card.colors[Card.rand.Next(4)];
                while (Card.color13.Contains("Red"))
                {
                    Card.color13 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color13">
                    Column
                </div>

                switch (Card.color13)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color13 = @Card.colors[Card.rand.Next(4)];
                while (Card.color13.Contains("Blue"))
                {
                    Card.color13 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color13">
                    Column
                </div>

                switch (Card.color13)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color13 = @Card.colors[Card.rand.Next(4)];
                while (Card.color13.Contains("White"))
                {
                    Card.color13 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color13">
                    Column
                </div>

                switch (Card.color13)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color13 = @Card.colors[Card.rand.Next(3)];
                while (Card.color13.Equals("Red"))
                {
                    Card.color13 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color13">
                    Column
                </div>



                switch (Card.color13)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color13 = @Card.colors[Card.rand.Next(3)];
                while (Card.color13.Equals("Blue"))
                {
                    Card.color13 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color13">
                    Column
                </div>

                switch (Card.color13)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color13 = @Card.colors[Card.rand.Next(3)];
                while (Card.color13.Equals("White"))
                {
                    Card.color13 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color13">
                    Column
                </div>

                switch (Card.color13)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color13 = @Card.colors[Card.rand.Next(4)];
                while (Card.color13.Equals("Red") || Card.color13.Equals("Blue"))
                {
                    Card.color13 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color13">
                    Column
                </div>

                switch (Card.color13)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color13 = @Card.colors[Card.rand.Next(4)];
                while (Card.color13.Equals("Red") || Card.color13.Equals("White"))
                {
                    Card.color13 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color13">
                    Column
                </div>

                switch (Card.color13)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color13 = @Card.colors[Card.rand.Next(4)];
                while (Card.color13.Equals("White") || Card.color13.Equals("Blue"))
                {
                    Card.color13 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color13">
                    Column
                </div>

                switch (Card.color13)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color13 = "Red";


                <div class="@Card.color13">
                    Column
                </div>


                if (Card.color13.Contains("Red"))
                {
                    Card.redColorCount++;
                    if (Card.redColorCount == 9)
                    {
                        Card.redComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color13 = "Blue";


                <div class="@Card.color13">
                    Column
                </div>


                if (Card.color13.Contains("Blue"))
                {
                    Card.blueColorCount++;
                    if (Card.blueColorCount == 8)
                    {
                        Card.blueComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color13 = "White";


                <div class="@Card.color13">
                    Column
                </div>


                if (Card.color13.Contains("White"))
                {
                    Card.whiteColorCount++;
                    if (Card.whiteColorCount == 7)
                    {
                        Card.whiteComplete = true;
                    }
                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color13 = "Black";


                <div class="@Card.color13">
                    Column
                </div>


                if (Card.color13.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color13">
                    Column
                </div>
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color14 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color14">
                    Column
                </div>

                switch (Card.color14)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color14 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color14">
                    Column
                </div>
                switch (Card.color14)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color14 = @Card.colors[Card.rand.Next(4)];
                while (Card.color14.Contains("Red"))
                {
                    Card.color14 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color14">
                    Column
                </div>

                switch (Card.color14)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color14 = @Card.colors[Card.rand.Next(4)];
                while (Card.color14.Contains("Blue"))
                {
                    Card.color14 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color14">
                    Column
                </div>

                switch (Card.color14)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color14 = @Card.colors[Card.rand.Next(4)];
                while (Card.color14.Contains("White"))
                {
                    Card.color14 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color14">
                    Column
                </div>

                switch (Card.color14)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color14 = @Card.colors[Card.rand.Next(3)];
                while (Card.color14.Equals("Red"))
                {
                    Card.color14 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color14">
                    Column
                </div>



                switch (Card.color14)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color14 = @Card.colors[Card.rand.Next(3)];
                while (Card.color14.Equals("Blue"))
                {
                    Card.color14 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color14">
                    Column
                </div>

                switch (Card.color14)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color14 = @Card.colors[Card.rand.Next(3)];
                while (Card.color14.Equals("White"))
                {
                    Card.color14 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color14">
                    Column
                </div>

                switch (Card.color14)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color14 = @Card.colors[Card.rand.Next(4)];
                while (Card.color14.Equals("Red") || Card.color14.Equals("Blue"))
                {
                    Card.color14 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color14">
                    Column
                </div>

                switch (Card.color14)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color14 = @Card.colors[Card.rand.Next(4)];
                while (Card.color14.Equals("Red") || Card.color14.Equals("White"))
                {
                    Card.color14 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color14">
                    Column
                </div>

                switch (Card.color14)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color14 = @Card.colors[Card.rand.Next(4)];
                while (Card.color14.Equals("White") || Card.color14.Equals("Blue"))
                {
                    Card.color14 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color14">
                    Column
                </div>

                switch (Card.color14)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color14 = "Red";


                <div class="@Card.color14">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color14 = "Blue";


                <div class="@Card.color14">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color14 = "White";


                <div class="@Card.color14">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color14 = "Black";


                <div class="@Card.color14">
                    Column
                </div>


                if (Card.color14.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color14">
                    Column
                </div>
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color15 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color15">
                    Column
                </div>

                switch (Card.color15)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color15 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color15">
                    Column
                </div>
                switch (Card.color15)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color15 = @Card.colors[Card.rand.Next(4)];
                while (Card.color15.Contains("Red"))
                {
                    Card.color15 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color15">
                    Column
                </div>

                switch (Card.color15)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color15 = @Card.colors[Card.rand.Next(4)];
                while (Card.color15.Contains("Blue"))
                {
                    Card.color15 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color15">
                    Column
                </div>

                switch (Card.color15)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color15 = @Card.colors[Card.rand.Next(4)];
                while (Card.color15.Contains("White"))
                {
                    Card.color15 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color15">
                    Column
                </div>

                switch (Card.color15)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color15 = @Card.colors[Card.rand.Next(3)];
                while (Card.color15.Equals("Red"))
                {
                    Card.color15 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color15">
                    Column
                </div>



                switch (Card.color15)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color15 = @Card.colors[Card.rand.Next(3)];
                while (Card.color15.Equals("Blue"))
                {
                    Card.color15 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color15">
                    Column
                </div>

                switch (Card.color15)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color15 = @Card.colors[Card.rand.Next(3)];
                while (Card.color15.Equals("White"))
                {
                    Card.color15 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color15">
                    Column
                </div>

                switch (Card.color15)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color15 = @Card.colors[Card.rand.Next(4)];
                while (Card.color15.Equals("Red") || Card.color15.Equals("Blue"))
                {
                    Card.color15 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color15">
                    Column
                </div>

                switch (Card.color15)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color15 = @Card.colors[Card.rand.Next(4)];
                while (Card.color15.Equals("Red") || Card.color15.Equals("White"))
                {
                    Card.color15 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color15">
                    Column
                </div>

                switch (Card.color15)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color15 = @Card.colors[Card.rand.Next(4)];
                while (Card.color15.Equals("White") || Card.color15.Equals("Blue"))
                {
                    Card.color15 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color15">
                    Column
                </div>

                switch (Card.color15)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color15 = "Red";


                <div class="@Card.color15">
                    Column
                </div>


                if (Card.color15.Contains("Red"))
                {
                    Card.redColorCount++;
                    if (Card.redColorCount == 9)
                    {
                        Card.redComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color15 = "Blue";


                <div class="@Card.color15">
                    Column
                </div>


                if (Card.color15.Contains("Blue"))
                {
                    Card.blueColorCount++;
                    if (Card.blueColorCount == 8)
                    {
                        Card.blueComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color15 = "White";


                <div class="@Card.color15">
                    Column
                </div>


                if (Card.color15.Contains("White"))
                {
                    Card.whiteColorCount++;
                    if (Card.whiteColorCount == 7)
                    {
                        Card.whiteComplete = true;
                    }
                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color15 = "Black";


                <div class="@Card.color15">
                    Column
                </div>


                if (Card.color15.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color15">
                    Column
                </div>
            }

        </div>
    </div>
    <div class="row p-2">
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color16 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color16">
                    Column
                </div>

                switch (Card.color16)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color16 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color16">
                    Column
                </div>
                switch (Card.color16)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color16 = @Card.colors[Card.rand.Next(4)];
                while (Card.color16.Contains("Red"))
                {
                    Card.color16 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color16">
                    Column
                </div>

                switch (Card.color16)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color16 = @Card.colors[Card.rand.Next(4)];
                while (Card.color16.Contains("Blue"))
                {
                    Card.color16 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color16">
                    Column
                </div>

                switch (Card.color16)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color16 = @Card.colors[Card.rand.Next(4)];
                while (Card.color16.Contains("White"))
                {
                    Card.color16 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color16">
                    Column
                </div>

                switch (Card.color16)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color16 = @Card.colors[Card.rand.Next(3)];
                while (Card.color16.Equals("Red"))
                {
                    Card.color16 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color16">
                    Column
                </div>



                switch (Card.color16)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color16 = @Card.colors[Card.rand.Next(3)];
                while (Card.color16.Equals("Blue"))
                {
                    Card.color16 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color16">
                    Column
                </div>

                switch (Card.color16)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color16 = @Card.colors[Card.rand.Next(3)];
                while (Card.color16.Equals("White"))
                {
                    Card.color16 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color16">
                    Column
                </div>

                switch (Card.color16)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color16 = @Card.colors[Card.rand.Next(4)];
                while (Card.color16.Equals("Red") || Card.color16.Equals("Blue"))
                {
                    Card.color16 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color16">
                    Column
                </div>

                switch (Card.color16)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color16 = @Card.colors[Card.rand.Next(4)];
                while (Card.color16.Equals("Red") || Card.color16.Equals("White"))
                {
                    Card.color16 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color16">
                    Column
                </div>

                switch (Card.color16)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
               Card.color16 = @Card.colors[Card.rand.Next(4)];
                while (Card.color16.Equals("White") || Card.color16.Equals("Blue"))
                {
                    Card.color16 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color16">
                    Column
                </div>

                switch (Card.color16)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color16 = "Red";


                <div class="@Card.color16">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color16 = "Blue";


                <div class="@Card.color16">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color16 = "White";


                <div class="@Card.color16">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color16 = "Black";


                <div class="@Card.color16">
                    Column
                </div>


                if (Card.color16.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color16">
                    Column
                </div>
            }
        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color17 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color17">
                    Column
                </div>

                switch (Card.color17)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color17 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color17">
                    Column
                </div>
                switch (Card.color17)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color17 = @Card.colors[Card.rand.Next(4)];
                while (Card.color17.Contains("Red"))
                {
                    Card.color17 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color17">
                    Column
                </div>

                switch (Card.color17)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color17 = @Card.colors[Card.rand.Next(4)];
                while (Card.color17.Contains("Blue"))
                {
                    Card.color17 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color17">
                    Column
                </div>

                switch (Card.color17)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color17 = @Card.colors[Card.rand.Next(4)];
                while (Card.color17.Contains("White"))
                {
                    Card.color17 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color17">
                    Column
                </div>

                switch (Card.color17)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color17 = @Card.colors[Card.rand.Next(3)];
                while (Card.color17.Equals("Red"))
                {
                    Card.color17 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color17">
                    Column
                </div>



                switch (Card.color17)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color17 = @Card.colors[Card.rand.Next(3)];
                while (Card.color17.Equals("Blue"))
                {
                    Card.color17 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color17">
                    Column
                </div>

                switch (Card.color17)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color17 = @Card.colors[Card.rand.Next(3)];
                while (Card.color17.Equals("White"))
                {
                    Card.color17 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color17">
                    Column
                </div>

                switch (Card.color17)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color17 = @Card.colors[Card.rand.Next(4)];
                while (Card.color17.Equals("Red") || Card.color17.Equals("Blue"))
                {
                    Card.color17 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color17">
                    Column
                </div>

                switch (Card.color17)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color17 = @Card.colors[Card.rand.Next(4)];
                while (Card.color17.Equals("Red") || Card.color17.Equals("White"))
                {
                    Card.color17 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color17">
                    Column
                </div>

                switch (Card.color17)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color17 = @Card.colors[Card.rand.Next(4)];
                while (Card.color17.Equals("White") || Card.color17.Equals("Blue"))
                {
                    Card.color17 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color17">
                    Column
                </div>

                switch (Card.color17)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color17 = "Red";


                <div class="@Card.color17">
                    Column
                </div>


                if (Card.color17.Contains("Red"))
                {
                    Card.redColorCount++;
                    if (Card.redColorCount == 9)
                    {
                        Card.redComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color17 = "Blue";


                <div class="@Card.color17">
                    Column
                </div>


                if (Card.color17.Contains("Blue"))
                {
                    Card.blueColorCount++;
                    if (Card.blueColorCount == 8)
                    {
                        Card.blueComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color17 = "White";


                <div class="@Card.color17">
                    Column
                </div>


                if (Card.color17.Contains("White"))
                {
                    Card.whiteColorCount++;
                    if (Card.whiteColorCount == 7)
                    {
                        Card.whiteComplete = true;
                    }
                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color17 = "Black";


                <div class="@Card.color17">
                    Column
                </div>


                if (Card.color17.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color17">
                    Column
                </div>
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color18 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color18">
                    Column
                </div>

                switch (Card.color18)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color18 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color18">
                    Column
                </div>
                switch (Card.color18)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color18 = @Card.colors[Card.rand.Next(4)];
                while (Card.color18.Contains("Red"))
                {
                    Card.color18 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color18">
                    Column
                </div>

                switch (Card.color18)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color18 = @Card.colors[Card.rand.Next(4)];
                while (Card.color18.Contains("Blue"))
                {
                    Card.color18 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color18">
                    Column
                </div>

                switch (Card.color18)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color18 = @Card.colors[Card.rand.Next(4)];
                while (Card.color18.Contains("White"))
                {
                    Card.color18 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color18">
                    Column
                </div>

                switch (Card.color18)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color18 = @Card.colors[Card.rand.Next(3)];
                while (Card.color18.Equals("Red"))
                {
                    Card.color18 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color18">
                    Column
                </div>



                switch (Card.color18)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color18 = @Card.colors[Card.rand.Next(3)];
                while (Card.color18.Equals("Blue"))
                {
                    Card.color18 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color18">
                    Column
                </div>

                switch (Card.color18)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color18 = @Card.colors[Card.rand.Next(3)];
                while (Card.color18.Equals("White"))
                {
                    Card.color18 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color18">
                    Column
                </div>

                switch (Card.color18)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color18 = @Card.colors[Card.rand.Next(4)];
                while (Card.color18.Equals("Red") || Card.color18.Equals("Blue"))
                {
                    Card.color18 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color18">
                    Column
                </div>

                switch (Card.color18)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color18 = @Card.colors[Card.rand.Next(4)];
                while (Card.color18.Equals("Red") || Card.color18.Equals("White"))
                {
                    Card.color18 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color18">
                    Column
                </div>

                switch (Card.color18)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color18 = @Card.colors[Card.rand.Next(4)];
                while (Card.color18.Equals("White") || Card.color18.Equals("Blue"))
                {
                    Card.color18 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color18">
                    Column
                </div>

                switch (Card.color18)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color18 = "Red";


                <div class="@Card.color18">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color18 = "Blue";


                <div class="@Card.color18">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color18 = "White";


                <div class="@Card.color18">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color18 = "Black";


                <div class="@Card.color18">
                    Column
                </div>


                if (Card.color18.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color18">
                    Column
                </div>
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color19 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color19">
                    Column
                </div>

                switch (Card.color19)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color19 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color19">
                    Column
                </div>
                switch (Card.color19)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
               Card.color19 = @Card.colors[Card.rand.Next(4)];
                while (Card.color19.Contains("Red"))
                {
                    Card.color19 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color19">
                    Column
                </div>

                switch (Card.color19)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color19 = @Card.colors[Card.rand.Next(4)];
                while (Card.color19.Contains("Blue"))
                {
                    Card.color19 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color19">
                    Column
                </div>

                switch (Card.color19)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color19 = @Card.colors[Card.rand.Next(4)];
                while (Card.color19.Contains("White"))
                {
                    Card.color19 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color19">
                    Column
                </div>

                switch (Card.color19)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color19 = @Card.colors[Card.rand.Next(3)];
                while (Card.color19.Equals("Red"))
                {
                    Card.color19 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color19">
                    Column
                </div>



                switch (Card.color19)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color19 = @Card.colors[Card.rand.Next(3)];
                while (Card.color19.Equals("Blue"))
                {
                    Card.color19 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color19">
                    Column
                </div>

                switch (Card.color19)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color19 = @Card.colors[Card.rand.Next(3)];
                while (Card.color19.Equals("White"))
                {
                    Card.color19 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color19">
                    Column
                </div>

                switch (Card.color19)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color19 = @Card.colors[Card.rand.Next(4)];
                while (Card.color19.Equals("Red") || Card.color19.Equals("Blue"))
                {
                    Card.color19 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color19">
                    Column
                </div>

                switch (Card.color19)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color19 = @Card.colors[Card.rand.Next(4)];
                while (Card.color19.Equals("Red") || Card.color19.Equals("White"))
                {
                    Card.color19 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color19">
                    Column
                </div>

                switch (Card.color19)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color19 = @Card.colors[Card.rand.Next(4)];
                while (Card.color19.Equals("White") || Card.color19.Equals("Blue"))
                {
                    Card.color19 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color19">
                    Column
                </div>

                switch (Card.color19)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
              else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color19 = "Red";
               

                <div class="@Card.color19">
                    Column
                </div>

               
                if (Card.color19.Contains("Red"))
                {
                    Card.redColorCount++;
                    if (Card.redColorCount == 9)
                    {
                        Card.redComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color19 = "Blue";


                <div class="@Card.color19">
                    Column
                </div>


                if (Card.color19.Contains("Blue"))
                {
                    Card.blueColorCount++;
                    if (Card.blueColorCount == 8)
                    {
                        Card.blueComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color19 = "White";


                <div class="@Card.color19">
                    Column
                </div>


                if (Card.color19.Contains("White"))
                {
                    Card.whiteColorCount++;
                    if (Card.whiteColorCount == 7)
                    {
                        Card.whiteComplete = true;
                    }
                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color19 = "Black";


                <div class="@Card.color19">
                    Column
                </div>


                if (Card.color19.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color19">
                    Column
                </div>
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color20 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color20">
                    Column
                </div>

                switch (Card.color20)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color20 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color20">
                    Column
                </div>
                switch (Card.color20)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color20 = @Card.colors[Card.rand.Next(4)];
                while (Card.color20.Contains("Red"))
                {
                    Card.color20 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color20">
                    Column
                </div>

                switch (Card.color20)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color20 = @Card.colors[Card.rand.Next(4)];
                while (Card.color20.Contains("Blue"))
                {
                    Card.color20 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color20">
                    Column
                </div>

                switch (Card.color20)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color20 = @Card.colors[Card.rand.Next(4)];
                while (Card.color20.Contains("White"))
                {
                    Card.color20 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color20">
                    Column
                </div>

                switch (Card.color20)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color20 = @Card.colors[Card.rand.Next(3)];
                while (Card.color20.Equals("Red"))
                {
                    Card.color20 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color20">
                    Column
                </div>



                switch (Card.color20)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color20 = @Card.colors[Card.rand.Next(3)];
                while (Card.color20.Equals("Blue"))
                {
                    Card.color20 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color20">
                    Column
                </div>

                switch (Card.color20)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color20 = @Card.colors[Card.rand.Next(3)];
                while (Card.color20.Equals("White"))
                {
                    Card.color20 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color20">
                    Column
                </div>

                switch (Card.color20)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color20 = @Card.colors[Card.rand.Next(4)];
                while (Card.color20.Equals("Red") || Card.color20.Equals("Blue"))
                {
                    Card.color20 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color20">
                    Column
                </div>

                switch (Card.color20)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color20 = @Card.colors[Card.rand.Next(4)];
                while (Card.color20.Equals("Red") || Card.color20.Equals("White"))
                {
                    Card.color20 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color20">
                    Column
                </div>

                switch (Card.color20)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color20 = @Card.colors[Card.rand.Next(4)];
                while (Card.color20.Equals("White") || Card.color20.Equals("Blue"))
                {
                    Card.color20 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color20">
                    Column
                </div>

                switch (Card.color20)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color20 = "Red";


                <div class="@Card.color20">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color20 = "Blue";


                <div class="@Card.color20">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color20 = "White";


                <div class="@Card.color20">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color20 = "Black";


                <div class="@Card.color20">
                    Column
                </div>


                if (Card.color20.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color20">
                    Column
                </div>
            }

        </div>
    </div>
    <div class="row p-2">
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color21 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color21">
                    Column
                </div>

                switch (Card.color21)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color21 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color21">
                    Column
                </div>
                switch (Card.color21)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color21 = @Card.colors[Card.rand.Next(4)];
                while (Card.color21.Contains("Red"))
                {
                    Card.color21 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color21">
                    Column
                </div>

                switch (Card.color21)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color21 = @Card.colors[Card.rand.Next(4)];
                while (Card.color21.Contains("Blue"))
                {
                    Card.color21 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color21">
                    Column
                </div>

                switch (Card.color21)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color21 = @Card.colors[Card.rand.Next(4)];
                while (Card.color21.Contains("White"))
                {
                    Card.color21 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color21">
                    Column
                </div>

                switch (Card.color21)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color21 = @Card.colors[Card.rand.Next(3)];
                while (Card.color21.Equals("Red"))
                {
                    Card.color21 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color21">
                    Column
                </div>



                switch (Card.color21)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color21 = @Card.colors[Card.rand.Next(3)];
                while (Card.color21.Equals("Blue"))
                {
                    Card.color21 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color21">
                    Column
                </div>

                switch (Card.color21)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color21 = @Card.colors[Card.rand.Next(3)];
                while (Card.color21.Equals("White"))
                {
                    Card.color21 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color21">
                    Column
                </div>

                switch (Card.color21)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color21 = @Card.colors[Card.rand.Next(4)];
                while (Card.color21.Equals("Red") || Card.color21.Equals("Blue"))
                {
                    Card.color21 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color21">
                    Column
                </div>

                switch (Card.color21)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color21 = @Card.colors[Card.rand.Next(4)];
                while (Card.color21.Equals("Red") || Card.color21.Equals("White"))
                {
                    Card.color21 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color21">
                    Column
                </div>

                switch (Card.color21)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color21 = @Card.colors[Card.rand.Next(4)];
                while (Card.color21.Equals("White") || Card.color21.Equals("Blue"))
                {
                    Card.color21 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color21">
                    Column
                </div>

                switch (Card.color21)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color21 = "Red";


                <div class="@Card.color21">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color21 = "Blue";


                <div class="@Card.color21">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color21 = "White";


                <div class="@Card.color21">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color21 = "Black";


                <div class="@Card.color21">
                    Column
                </div>


                if (Card.color21.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color21">
                    Column
                </div>
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color22 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color22">
                    Column
                </div>

                switch (Card.color22)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color22 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color22">
                    Column
                </div>
                switch (Card.color22)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color22 = @Card.colors[Card.rand.Next(4)];
                while (Card.color22.Contains("Red"))
                {
                    Card.color22 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color22">
                    Column
                </div>

                switch (Card.color22)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color22 = @Card.colors[Card.rand.Next(4)];
                while (Card.color22.Contains("Blue"))
                {
                    Card.color22 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color22">
                    Column
                </div>

                switch (Card.color22)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color22 = @Card.colors[Card.rand.Next(4)];
                while (Card.color22.Contains("White"))
                {
                    Card.color22 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color22">
                    Column
                </div>

                switch (Card.color22)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color22 = @Card.colors[Card.rand.Next(3)];
                while (Card.color22.Equals("Red"))
                {
                    Card.color22 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color22">
                    Column
                </div>



                switch (Card.color22)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color22 = @Card.colors[Card.rand.Next(3)];
                while (Card.color22.Equals("Blue"))
                {
                    Card.color22 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color22">
                    Column
                </div>

                switch (Card.color22)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color22 = @Card.colors[Card.rand.Next(3)];
                while (Card.color22.Equals("White"))
                {
                    Card.color22 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color22">
                    Column
                </div>

                switch (Card.color22)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color22 = @Card.colors[Card.rand.Next(4)];
                while (Card.color22.Equals("Red") || Card.color22.Equals("Blue"))
                {
                    Card.color22 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color22">
                    Column
                </div>

                switch (Card.color22)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color22 = @Card.colors[Card.rand.Next(4)];
                while (Card.color22.Equals("Red") || Card.color22.Equals("White"))
                {
                    Card.color22 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color22">
                    Column
                </div>

                switch (Card.color22)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color22 = @Card.colors[Card.rand.Next(4)];
                while (Card.color22.Equals("White") || Card.color22.Equals("Blue"))
                {
                    Card.color22 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color22">
                    Column
                </div>

                switch (Card.color22)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color22 = "Red";
               

                <div class="@Card.color22">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }
                
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color22 = "Blue";


                <div class="@Card.color22">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }
                
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color22 = "White";


                <div class="@Card.color22">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }
                
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color22 = "Black";


                <div class="@Card.color22">
                    Column
                </div>


                if (Card.color22.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color22">
                    Column
                </div>
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color23">
                    Column
                </div>

                switch (Card.color23)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color23">
                    Column
                </div>
                switch (Card.color23)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(4)];
                while (Card.color23.Contains("Red"))
                {
                    Card.color23 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color23">
                    Column
                </div>

                switch (Card.color23)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(4)];
                while (Card.color23.Contains("Blue"))
                {
                    Card.color23 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color23">
                    Column
                </div>

                switch (Card.color23)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color23= @Card.colors[Card.rand.Next(4)];
                while (Card.color23.Contains("White"))
                {
                    Card.color23 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color23">
                    Column
                </div>

                switch (Card.color23)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(3)];
                while (Card.color23.Equals("Red"))
                {
                    Card.color23 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color23">
                    Column
                </div>



                switch (Card.color23)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(3)];
                while (Card.color23.Equals("Blue"))
                {
                    Card.color23 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color23">
                    Column
                </div>

                switch (Card.color23)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(3)];
                while (Card.color23.Equals("White"))
                {
                    Card.color23 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color23">
                    Column
                </div>

                switch (Card.color23)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(4)];
                while (Card.color23.Equals("Red") || Card.color23.Equals("Blue"))
                {
                    Card.color23 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color23">
                    Column
                </div>

                switch (Card.color23)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(4)];
                while (Card.color23.Equals("Red") || Card.color23.Equals("White"))
                {
                    Card.color23 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color23">
                    Column
                </div>

                switch (Card.color23)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(4)];
                while (Card.color23.Equals("White") || Card.color23.Equals("Blue"))
                {
                    Card.color23 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color23">
                    Column
                </div>

                switch (Card.color23)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color23 = "Red";


                <div class="@Card.color23">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color23 = "Blue";


                <div class="@Card.color23">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color23 = "White";


                <div class="@Card.color23">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color23 = "Black";


                <div class="@Card.color23">
                    Column
                </div>


                if (Card.color23.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                 <div class="@Card.color23">
                    Column
                </div>
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color23">
                    Column
                </div>

                switch (Card.color23)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color23">
                    Column
                </div>
                switch (Card.color23)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(4)];
                while (Card.color23.Contains("Red"))
                {
                    Card.color23 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color23">
                    Column
                </div>

                switch (Card.color23)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(4)];
                while (Card.color23.Contains("Blue"))
                {
                    Card.color23 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color23">
                    Column
                </div>

                switch (Card.color23)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(4)];
                while (Card.color23.Contains("White"))
                {
                    Card.color23 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color23">
                    Column
                </div>

                switch (Card.color23)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(3)];
                while (Card.color23.Equals("Red"))
                {
                    Card.color23 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color23">
                    Column
                </div>



                switch (Card.color23)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(3)];
                while (Card.color23.Equals("Blue"))
                {
                    Card.color23 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color23">
                    Column
                </div>

                switch (Card.color23)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(3)];
                while (Card.color23.Equals("White"))
                {
                    Card.color23 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color23">
                    Column
                </div>

                switch (Card.color23)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(4)];
                while (Card.color23.Equals("Red") || Card.color23.Equals("Blue"))
                {
                    Card.color23 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color23">
                    Column
                </div>

                switch (Card.color23)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(4)];
                while (Card.color23.Equals("Red") || Card.color23.Equals("White"))
                {
                    Card.color23 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color23">
                    Column
                </div>

                switch (Card.color23)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color23 = @Card.colors[Card.rand.Next(4)];
                while (Card.color23.Equals("White") || Card.color23.Equals("Blue"))
                {
                    Card.color23 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color23">
                    Column
                </div>

                switch (Card.color23)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color23 = "Red";


                <div class="@Card.color23">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color23 = "Blue";


                <div class="@Card.color23">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color23 = "White";


                <div class="@Card.color23">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color23 = "Black";


                <div class="@Card.color23">
                    Column
                </div>


                if (Card.color23.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color23">
                    Column
                </div>
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color24 = @Card.colors[Card.rand.Next(4)];

                <div class="@Card.color24">
                    Column
                </div>

                switch (Card.color24)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color24 = @Card.colors[Card.rand.Next(3)];

                <div class="@Card.color24">
                    Column
                </div>
                switch (Card.color24)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color24 = @Card.colors[Card.rand.Next(4)];
                while (Card.color24.Contains("Red"))
                {
                    Card.color24 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color24">
                    Column
                </div>

                switch (Card.color24)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color24 = @Card.colors[Card.rand.Next(4)];
                while (Card.color24.Contains("Blue"))
                {
                    Card.color24 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color24">
                    Column
                </div>

                switch (Card.color24)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color24 = @Card.colors[Card.rand.Next(4)];
                while (Card.color24.Contains("White"))
                {
                    Card.color24 = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@Card.color24">
                    Column
                </div>

                switch (Card.color24)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                Card.color24 = @Card.colors[Card.rand.Next(3)];
                while (Card.color24.Equals("Red"))
                {
                    Card.color24 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color24">
                    Column
                </div>



                switch (Card.color24)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color24 = @Card.colors[Card.rand.Next(3)];
                while (Card.color24.Equals("Blue"))
                {
                    Card.color24 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color24">
                    Column
                </div>

                switch (Card.color24)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color24 = @Card.colors[Card.rand.Next(3)];
                while (Card.color24.Equals("White"))
                {
                    Card.color24 = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@Card.color24">
                    Column
                </div>

                switch (Card.color24)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color24 = @Card.colors[Card.rand.Next(4)];
                while (Card.color24.Equals("Red") || Card.color24.Equals("Blue"))
                {
                    Card.color24 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color24">
                    Column
                </div>

                switch (Card.color24)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color24 = @Card.colors[Card.rand.Next(4)];
                while (Card.color24.Equals("Red") || Card.color24.Equals("White"))
                {
                    Card.color24 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color24">
                    Column
                </div>

                switch (Card.color24)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color24 = @Card.colors[Card.rand.Next(4)];
                while (Card.color24.Equals("White") || Card.color24.Equals("Blue"))
                {
                    Card.color24 = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@Card.color24">
                    Column
                </div>

                switch (Card.color24)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color24 = "Red";


                <div class="@Card.color24">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                Card.color24 = "Blue";


                <div class="@Card.color24">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                Card.color24 = "White";


                <div class="@Card.color24">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                Card.color24 = "Black";


                <div class="@Card.color24">
                    Column
                </div>


                if (Card.color24.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                <div class="@Card.color24">
                    Column
                </div>
            }

        </div>
    </div>
</div>