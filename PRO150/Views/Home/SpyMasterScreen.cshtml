@model Card

<div class="text-center">
    <h2>You Are A Spy Master</h2>
    </br>

    <a href="GuesserTran"><button class="btn btn-primary">I've Given My Hint</button></a>
</div>

<div class="container text-center">
    <div class="row p-2">
        <div class="col border-2 border-primary p-5 m-3">
          
            @if(!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete )
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                
            }

              
            
        </div>
        <div class="col border-2 border-primary p-5 m-3">

            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                 switch (color)
                {
                    
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                  switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                   
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                 switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                 switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                

                 switch (color)
                {
                   
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                 switch (color)
                {
                   
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                   
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                 switch (color)
                {
                  
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                   


                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                  
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                 switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                  
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Red"))
                {
                    Card.redColorCount++;
                    if (Card.redColorCount == 9)
                    {
                        Card.redComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Blue"))
                {
                    Card.blueColorCount++;
                    if (Card.blueColorCount == 8)
                    {
                        Card.blueComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("White"))
                {
                    Card.whiteColorCount++;
                    if (Card.whiteColorCount == 7)
                    {
                        Card.whiteComplete = true;
                    }
                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }

        </div>
    </div>
    <div class="row p-2">
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Red"))
                {
                    Card.redColorCount++;
                    if (Card.redColorCount == 9)
                    {
                        Card.redComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Blue"))
                {
                    Card.blueColorCount++;
                    if (Card.blueColorCount == 8)
                    {
                        Card.blueComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("White"))
                {
                    Card.whiteColorCount++;
                    if (Card.whiteColorCount == 7)
                    {
                        Card.whiteComplete = true;
                    }
                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Red"))
                {
                    Card.redColorCount++;
                    if (Card.redColorCount == 9)
                    {
                        Card.redComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }
        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Red"))
                {
                    Card.redColorCount++;
                    if (Card.redColorCount == 9)
                    {
                        Card.redComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Blue"))
                {
                    Card.blueColorCount++;
                    if (Card.blueColorCount == 8)
                    {
                        Card.blueComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("White"))
                {
                    Card.whiteColorCount++;
                    if (Card.whiteColorCount == 7)
                    {
                        Card.whiteComplete = true;
                    }
                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Red"))
                {
                    Card.redColorCount++;
                    if (Card.redColorCount == 9)
                    {
                        Card.redComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Blue"))
                {
                    Card.blueColorCount++;
                    if (Card.blueColorCount == 8)
                    {
                        Card.blueComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("White"))
                {
                    Card.whiteColorCount++;
                    if (Card.whiteColorCount == 7)
                    {
                        Card.whiteComplete = true;
                    }
                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }

        </div>
    </div>
    <div class="row p-2">
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
          else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";
               

                <div class="@color">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }
                
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }
                
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }
                
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">

            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Red"))
                {
                    Card.redColorCount++;
                    if (Card.redColorCount == 9)
                    {
                        Card.redComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Blue"))
                {
                    Card.blueColorCount++;
                    if (Card.blueColorCount == 8)
                    {
                        Card.blueComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("White"))
                {
                    Card.whiteColorCount++;
                    if (Card.whiteColorCount == 7)
                    {
                        Card.whiteComplete = true;
                    }
                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Red"))
                {
                    Card.redColorCount++;
                    if (Card.redColorCount == 9)
                    {
                        Card.redComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Blue"))
                {
                    Card.blueColorCount++;
                    if (Card.blueColorCount == 8)
                    {
                        Card.blueComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("White"))
                {
                    Card.whiteColorCount++;
                    if (Card.whiteColorCount == 7)
                    {
                        Card.whiteComplete = true;
                    }
                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }

        </div>
    </div>
    <div class="row p-2">
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }
        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Red"))
                {
                    Card.redColorCount++;
                    if (Card.redColorCount == 9)
                    {
                        Card.redComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Blue"))
                {
                    Card.blueColorCount++;
                    if (Card.blueColorCount == 8)
                    {
                        Card.blueComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("White"))
                {
                    Card.whiteColorCount++;
                    if (Card.whiteColorCount == 7)
                    {
                        Card.whiteComplete = true;
                    }
                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
              else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";
               

                <div class="@color">
                    Column
                </div>

               
                if (color.Contains("Red"))
                {
                    Card.redColorCount++;
                    if (Card.redColorCount == 9)
                    {
                        Card.redComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Blue"))
                {
                    Card.blueColorCount++;
                    if (Card.blueColorCount == 8)
                    {
                        Card.blueComplete = true;
                    }
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("White"))
                {
                    Card.whiteColorCount++;
                    if (Card.whiteColorCount == 7)
                    {
                        Card.whiteComplete = true;
                    }
                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }

        </div>
    </div>
    <div class="row p-2">
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";
               

                <div class="@color">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }
                
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }
                
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }
                
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                
            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }

        </div>
        <div class="col border-2 border-primary p-5 m-3">


            @if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];

                <div class="@color">
                    Column
                </div>
                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Red"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Contains("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];
                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;

                }
            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Red"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>



                switch (color)
                {

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;
                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (Card.isAssassin && !Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(3)];
                while (color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(3)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {

                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "White":
                        Card.whiteColorCount++;
                        if (Card.whiteColorCount == 7)
                        {
                            Card.whiteComplete = true;
                        }
                        break;


                }
            }
            else if (!Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("Red") || color.Equals("White"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;

                    case "Blue":
                        Card.blueColorCount++;
                        if (Card.blueColorCount == 8)
                        {
                            Card.blueComplete = true;
                        }
                        break;



                }
            }
            else if (!Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = @Card.colors[Card.rand.Next(4)];
                while (color.Equals("White") || color.Equals("Blue"))
                {
                    color = @Card.colors[Card.rand.Next(4)];

                }

                <div class="@color">
                    Column
                </div>

                switch (color)
                {
                    case "Black":
                        Card.isAssassin = true;
                        break;
                    case "Red":
                        Card.redColorCount++;
                        if (Card.redColorCount == 9)
                        {
                            Card.redComplete = true;
                        }
                        break;
                }
            }
            else if (Card.isAssassin && !Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Red";


                <div class="@color">
                    Column
                </div>

                Card.redColorCount++;
                if (Card.redColorCount == 9)
                {
                    Card.redComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && !Card.blueComplete && Card.whiteComplete)
            {
                string color = "Blue";


                <div class="@color">
                    Column
                </div>
                Card.blueColorCount++;
                if (Card.blueColorCount == 8)
                {
                    Card.blueComplete = true;
                }

            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && !Card.whiteComplete)
            {
                string color = "White";


                <div class="@color">
                    Column
                </div>
                Card.whiteColorCount++;
                if (Card.whiteColorCount == 7)
                {
                    Card.whiteComplete = true;
                }

            }
            else if (!Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {
                string color = "Black";


                <div class="@color">
                    Column
                </div>


                if (color.Contains("Black"))
                {
                    Card.isAssassin = true;
                }
            }
            else if (Card.isAssassin && Card.redComplete && Card.blueComplete && Card.whiteComplete)
            {

            }

        </div>
    </div>
</div>